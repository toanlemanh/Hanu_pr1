package tut08;

public enum SIZE {
    SMALL ,
    MEDIUM,
    LARGE,
    EXTRA_LARGE;
    // enum is a special class but it cannot creat a object and cannot extend the classes
    // Also, contains attributes and method
    // enum contains constants are public, static and final
    //

}

